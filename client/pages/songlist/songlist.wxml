<view>
  <image src="{{coverImg}}" class="cover"/>
  <view class="container">
    <view
      wx:for="{{songList}}" 
      wx:key="{{item.id}}"
      class="list"
    >
      <view>{{item.name}}</view>
      <view>{{item.author}}</view>
    </view>
  </view>
</view>